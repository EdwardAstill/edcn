// AUTO-GENERATED by scripts/generate-preview-index.ts — do not edit.
 

import type { ComponentType } from "react";

import * as HelloWorldModule from "@/registry/blocks/hello-world/hello-world";
import * as ExampleFormModule from "@/registry/blocks/example-form/example-form";
import * as ComplexComponentModule from "@/registry/blocks/complex-component/pokemon-grid";
import * as FloatingWindowDemoModule from "@/registry/blocks/floating-window-demo/floating-window-demo";
import * as WorkspaceDemoModule from "@/registry/blocks/workspace/workspace-demo";
import * as ExampleWithCssModule from "@/registry/blocks/example-with-css/example-card";

export type PreviewFile = {
  path: string;
  source: string;
};

export type BlockDemo = {
  name: string;
  title: string;
  description: string;
  files: PreviewFile[];
  Component: ComponentType | null;
};

function resolveExport(
  moduleNamespace: Record<string, unknown>,
  name: string,
): ComponentType | null {
  const candidate = moduleNamespace[name] ?? moduleNamespace.default;
  if (candidate == null) return null;
  return candidate as ComponentType;
}

export const blockDemos: BlockDemo[] = [
  { name: "hello-world", title: "Hello World", description: "A simple hello world component", files: [{"path":"registry/blocks/hello-world/hello-world.tsx","source":"export function HelloWorld() {\n  return <h1 className=\"text-2xl font-bold\">Hello World</h1>\n}\n"}], Component: resolveExport(HelloWorldModule, "HelloWorld") },
  { name: "example-form", title: "Example Form", description: "A contact form with Zod validation.", files: [{"path":"registry/blocks/example-form/example-form.tsx","source":"\"use client\";\n\nimport * as React from \"react\";\nimport {\n  Card,\n  CardTitle,\n  CardHeader,\n  CardDescription,\n  CardContent,\n  CardFooter,\n} from \"@/registry/ui/card\";\nimport { Input } from \"@/registry/ui/input\";\nimport { Label } from \"@/registry/ui/label\";\nimport { Button } from \"@/registry/ui/button\";\nimport { Textarea } from \"@/registry/ui/textarea\";\nimport { z } from \"zod\";\n\nconst exampleFormSchema = z.object({\n  name: z.string().min(1),\n  email: z.string().email(),\n  message: z.string().min(1),\n});\n\nexport function ExampleForm() {\n  const [pending, setPending] = React.useState(false);\n  const [state, setState] = React.useState({\n    defaultValues: {\n      name: \"\",\n      email: \"\",\n      message: \"\",\n    },\n    success: false,\n    errors: {\n      name: \"\",\n      email: \"\",\n      message: \"\",\n    },\n  });\n\n  const handleSubmit = React.useCallback(\n    (e: React.FormEvent<HTMLFormElement>) => {\n      e.preventDefault();\n      setPending(true);\n\n      const formData = new FormData(e.target as HTMLFormElement);\n      const data = Object.fromEntries(formData.entries());\n      const result = exampleFormSchema.safeParse(data);\n\n      if (!result.success) {\n        setState({\n          ...state,\n          errors: Object.fromEntries(\n            Object.entries(result.error.flatten().fieldErrors).map(\n              ([key, value]) => [key, value?.[0] ?? \"\"],\n            ),\n          ) as Record<keyof typeof state.errors, string>,\n        });\n        setPending(false);\n        return;\n      }\n\n      setPending(false);\n    },\n    [state],\n  );\n\n  return (\n    <form onSubmit={handleSubmit} className=\"w-full max-w-sm\">\n      <Card>\n        <CardHeader>\n          <CardTitle>How can we help?</CardTitle>\n          <CardDescription>\n            Need help with your project? We&apos;re here to assist you.\n          </CardDescription>\n        </CardHeader>\n        <CardContent className=\"flex flex-col gap-6\">\n          <div\n            className=\"group/field grid gap-2\"\n            data-invalid={!!state.errors?.name}\n          >\n            <Label\n              htmlFor=\"name\"\n              className=\"group-data-[invalid=true]/field:text-destructive\"\n            >\n              Name <span aria-hidden=\"true\">*</span>\n            </Label>\n            <Input\n              id=\"name\"\n              name=\"name\"\n              placeholder=\"Lee Robinson\"\n              className=\"group-data-[invalid=true]/field:border-destructive focus-visible:group-data-[invalid=true]/field:ring-destructive\"\n              disabled={pending}\n              aria-invalid={!!state.errors?.name}\n              aria-errormessage=\"error-name\"\n              defaultValue={state.defaultValues.name}\n            />\n            {state.errors?.name && (\n              <p id=\"error-name\" className=\"text-destructive text-sm\">\n                {state.errors.name}\n              </p>\n            )}\n          </div>\n          <div\n            className=\"group/field grid gap-2\"\n            data-invalid={!!state.errors?.email}\n          >\n            <Label\n              htmlFor=\"email\"\n              className=\"group-data-[invalid=true]/field:text-destructive\"\n            >\n              Email <span aria-hidden=\"true\">*</span>\n            </Label>\n            <Input\n              id=\"email\"\n              name=\"email\"\n              placeholder=\"you@example.com\"\n              className=\"group-data-[invalid=true]/field:border-destructive focus-visible:group-data-[invalid=true]/field:ring-destructive\"\n              disabled={pending}\n              aria-invalid={!!state.errors?.email}\n              aria-errormessage=\"error-email\"\n              defaultValue={state.defaultValues.email}\n            />\n            {state.errors?.email && (\n              <p id=\"error-email\" className=\"text-destructive text-sm\">\n                {state.errors.email}\n              </p>\n            )}\n          </div>\n          <div\n            className=\"group/field grid gap-2\"\n            data-invalid={!!state.errors?.message}\n          >\n            <Label\n              htmlFor=\"message\"\n              className=\"group-data-[invalid=true]/field:text-destructive\"\n            >\n              Message <span aria-hidden=\"true\">*</span>\n            </Label>\n            <Textarea\n              id=\"message\"\n              name=\"message\"\n              placeholder=\"Type your message here...\"\n              className=\"group-data-[invalid=true]/field:border-destructive focus-visible:group-data-[invalid=true]/field:ring-destructive\"\n              disabled={pending}\n              aria-invalid={!!state.errors?.message}\n              aria-errormessage=\"error-message\"\n              defaultValue={state.defaultValues.message}\n            />\n            {state.errors?.message && (\n              <p id=\"error-message\" className=\"text-destructive text-sm\">\n                {state.errors.message}\n              </p>\n            )}\n          </div>\n        </CardContent>\n        <CardFooter>\n          <Button type=\"submit\" size=\"sm\" disabled={pending}>\n            {pending ? \"Sending...\" : \"Send Message\"}\n          </Button>\n        </CardFooter>\n      </Card>\n    </form>\n  );\n}\n"}], Component: resolveExport(ExampleFormModule, "ExampleForm") },
  { name: "complex-component", title: "Complex Component", description: "A complex component showing hooks, libs and components.", files: [{"path":"registry/blocks/complex-component/pokemon-list.tsx","source":"import { cache } from \"react\";\nimport { PokemonCard } from \"@/registry/blocks/complex-component/components/pokemon-card\";\nimport { getPokemonList } from \"@/registry/blocks/complex-component/lib/pokemon\";\nconst getCachedPokemonList = cache(getPokemonList);\n\nexport default async function Page() {\n  const pokemons = await getCachedPokemonList({ limit: 12 });\n\n  if (!pokemons) {\n    return null;\n  }\n\n  return (\n    <div className=\"mx-auto w-full max-w-2xl px-4\">\n      <div className=\"grid grid-cols-2 gap-4 py-10 sm:grid-cols-3 md:grid-cols-4\">\n        {pokemons.results.map((p) => (\n          <PokemonCard key={p.name} name={p.name} />\n        ))}\n      </div>\n    </div>\n  );\n}\n"},{"path":"registry/blocks/complex-component/pokemon-grid.tsx","source":"import { useEffect, useState } from \"react\";\nimport {\n  getPokemon,\n  getPokemonList,\n} from \"@/registry/blocks/complex-component/lib/pokemon\";\nimport { PokemonImage } from \"@/registry/blocks/complex-component/components/pokemon-image\";\nimport { Card, CardContent } from \"@/registry/ui/card\";\n\n// Client-side version of the complex-component block for browser previews.\n// The block's entry (pokemon-list.tsx) is an async server component,\n// which only renders inside RSC hosts like Next.js.\n\nexport function PokemonGrid() {\n  const [names, setNames] = useState<string[]>([]);\n\n  useEffect(() => {\n    getPokemonList({ limit: 12 }).then((result) => {\n      if (result) setNames(result.results.map((p) => p.name));\n    });\n  }, []);\n\n  if (names.length === 0) {\n    return null;\n  }\n\n  return (\n    <div className=\"mx-auto w-full max-w-2xl px-4\">\n      <div className=\"grid grid-cols-2 gap-4 py-10 sm:grid-cols-3 md:grid-cols-4\">\n        {names.map((name) => (\n          <PreviewPokemonCard key={name} name={name} />\n        ))}\n      </div>\n    </div>\n  );\n}\n\nfunction PreviewPokemonCard({ name }: { name: string }) {\n  const [pokemon, setPokemon] =\n    useState<Awaited<ReturnType<typeof getPokemon>>>(null);\n\n  useEffect(() => {\n    getPokemon(name).then(setPokemon);\n  }, [name]);\n\n  if (!pokemon) {\n    return null;\n  }\n\n  return (\n    <Card>\n      <CardContent className=\"flex flex-col items-center p-2\">\n        <div>\n          <PokemonImage name={pokemon.name} number={pokemon.id} />\n        </div>\n        <div className=\"text-center font-medium\">{pokemon.name}</div>\n      </CardContent>\n    </Card>\n  );\n}\n"},{"path":"registry/blocks/complex-component/components/pokemon-card.tsx","source":"import { cache } from \"react\";\nimport { getPokemon } from \"@/registry/blocks/complex-component/lib/pokemon\";\nimport { Card, CardContent } from \"@/registry/ui/card\";\nimport { PokemonImage } from \"@/registry/blocks/complex-component/components/pokemon-image\";\n\nconst cachedGetPokemon = cache(getPokemon);\n\nexport async function PokemonCard({ name }: { name: string }) {\n  const pokemon = await cachedGetPokemon(name);\n\n  if (!pokemon) {\n    return null;\n  }\n\n  return (\n    <Card>\n      <CardContent className=\"flex flex-col items-center p-2\">\n        <div>\n          <PokemonImage name={pokemon.name} number={pokemon.id} />\n        </div>\n        <div className=\"text-center font-medium\">{pokemon.name}</div>\n      </CardContent>\n    </Card>\n  );\n}\n"},{"path":"registry/blocks/complex-component/components/pokemon-image.tsx","source":"\"use client\";\n\nimport { usePokemonImage } from \"@/registry/blocks/complex-component/hooks/use-pokemon\";\n\nexport function PokemonImage({\n  name,\n  number,\n}: {\n  name: string;\n  number: number;\n}) {\n  const imageUrl = usePokemonImage(number);\n\n  if (!imageUrl) {\n    return null;\n  }\n\n  return <img src={imageUrl} alt={name} />;\n}\n"},{"path":"registry/blocks/complex-component/lib/pokemon.ts","source":"import { z } from \"zod\"\n\nexport async function getPokemonList({ limit = 10 }: { limit?: number }) {\n  try {\n    const response = await fetch(\n      `https://pokeapi.co/api/v2/pokemon?limit=${limit}`\n    )\n    return z\n      .object({\n        results: z.array(z.object({ name: z.string() })),\n      })\n      .parse(await response.json())\n  } catch (error) {\n    console.error(error)\n    return null\n  }\n}\n\nexport async function getPokemon(name: string) {\n  try {\n    const response = await fetch(`https://pokeapi.co/api/v2/pokemon/${name}`)\n\n    if (!response.ok) {\n      throw new Error(\"Failed to fetch pokemon\")\n    }\n\n    return z\n      .object({\n        name: z.string(),\n        id: z.number(),\n        sprites: z.object({\n          front_default: z.string(),\n        }),\n        stats: z.array(\n          z.object({\n            base_stat: z.number(),\n            stat: z.object({\n              name: z.string(),\n            }),\n          })\n        ),\n      })\n      .parse(await response.json())\n  } catch (error) {\n    console.error(error)\n    return null\n  }\n}\n"},{"path":"registry/blocks/complex-component/hooks/use-pokemon.ts","source":"\"use client\"\n\n// Totally unnecessary hook, but it's a good example of how to use a hook in a custom registry.\n\nexport function usePokemonImage(number: number) {\n  return `https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/${number}.png`\n}\n"}], Component: resolveExport(ComplexComponentModule, "PokemonGrid") },
  { name: "floating-window-demo", title: "Floating Window Demo", description: "Draggable, resizable floating windows.", files: [{"path":"registry/blocks/floating-window-demo/floating-window-demo.tsx","source":"import * as React from \"react\";\n\nimport { FloatingWindow } from \"@/registry/ui/floating-window\";\nimport { Button } from \"@/registry/ui/button\";\n\nexport function FloatingWindowDemo() {\n  return (\n    <div className=\"absolute inset-0\">\n      <FloatingWindow\n        title=\"Notes\"\n        defaultPosition={{ x: 16, y: 16 }}\n        defaultSize={{ width: 280, height: 170 }}\n      >\n        <p className=\"text-sm text-muted-foreground\">\n          Drag the title bar to move me. Grab any edge or corner to resize.\n        </p>\n      </FloatingWindow>\n      <FloatingWindow\n        title=\"Counter\"\n        defaultPosition={{ x: 340, y: 60 }}\n        defaultSize={{ width: 230, height: 160 }}\n        minWidth={190}\n        minHeight={130}\n      >\n        <Counter />\n      </FloatingWindow>\n    </div>\n  );\n}\n\nfunction Counter() {\n  const [count, setCount] = React.useState(0);\n\n  return (\n    <div className=\"flex h-full flex-col items-center justify-center gap-3\">\n      <span className=\"text-2xl font-bold tabular-nums\">{count}</span>\n      <div className=\"flex gap-2\">\n        <Button\n          variant=\"outline\"\n          size=\"sm\"\n          onClick={() => setCount((count) => count - 1)}\n        >\n          -1\n        </Button>\n        <Button size=\"sm\" onClick={() => setCount((count) => count + 1)}>\n          +1\n        </Button>\n      </div>\n    </div>\n  );\n}\n"},{"path":"registry/ui/floating-window.tsx","source":"/**\n * Floating window shell. Works controlled (position/size props + change\n * callbacks — geometry owned by workspace state) or uncontrolled (default*\n * props + internal state — standalone usage like the floating-window demo).\n *\n * Chrome (title bar) is optional: frameless windows are moved by holding\n * the configured modifier key (e.g. ctrl) and dragging anywhere.\n */\n\nimport * as React from \"react\";\n\nimport { cn } from \"@/lib/utils\";\nimport type { ModifierKey } from \"@/registry/lib/workspace/keymap\";\nimport { modifierToEventKey } from \"@/registry/lib/workspace/keymap\";\n\ntype ResizeEdge = \"n\" | \"s\" | \"e\" | \"w\" | \"ne\" | \"nw\" | \"se\" | \"sw\";\n\ninterface Position {\n  x: number;\n  y: number;\n}\n\ninterface Size {\n  width: number;\n  height: number;\n}\n\ninterface DragState {\n  edge: \"move\" | ResizeEdge;\n  startX: number;\n  startY: number;\n  originPosition: Position;\n  originSize: Size;\n}\n\nexport interface FloatingWindowProps\n  extends Omit<React.HTMLAttributes<HTMLDivElement>, \"onDragStart\"> {\n  title?: string;\n  /** Controlled position/size — geometry lives outside the component. */\n  position?: Position;\n  size?: Size;\n  /** Uncontrolled fallbacks used when position/size are omitted. */\n  defaultPosition?: Position;\n  defaultSize?: Size;\n  onPositionChange?: (position: Position) => void;\n  onSizeChange?: (size: Size) => void;\n  /** Show the title bar? Default true. */\n  chrome?: boolean;\n  /** Modifier held on pointer-down anywhere to move the window. */\n  dragModifier?: ModifierKey | null;\n  /** Stacking order applied as z-index. */\n  zIndex?: number;\n  onClose?: () => void;\n  /** Override the content area padding (e.g. \"p-0\" to fill edge to edge). */\n  contentClassName?: string;\n  minWidth?: number;\n  minHeight?: number;\n}\n\nconst resizeHandles: { edge: ResizeEdge; className: string }[] = [\n  { edge: \"n\", className: \"inset-x-3 top-0 h-1.5 cursor-ns-resize\" },\n  { edge: \"s\", className: \"inset-x-3 bottom-0 h-1.5 cursor-ns-resize\" },\n  { edge: \"w\", className: \"inset-y-3 left-0 w-1.5 cursor-ew-resize\" },\n  { edge: \"e\", className: \"inset-y-3 right-0 w-1.5 cursor-ew-resize\" },\n  { edge: \"nw\", className: \"left-0 top-0 size-3.5 cursor-nwse-resize\" },\n  { edge: \"ne\", className: \"right-0 top-0 size-3.5 cursor-nesw-resize\" },\n  { edge: \"sw\", className: \"bottom-0 left-0 size-3.5 cursor-nesw-resize\" },\n  { edge: \"se\", className: \"bottom-0 right-0 size-3.5 cursor-nwse-resize\" },\n];\n\nfunction FloatingWindow({\n  title,\n  position: positionProp,\n  size: sizeProp,\n  defaultPosition = { x: 40, y: 40 },\n  defaultSize = { width: 320, height: 220 },\n  onPositionChange,\n  onSizeChange,\n  chrome = true,\n  dragModifier,\n  zIndex,\n  onClose,\n  contentClassName,\n  minWidth = 220,\n  minHeight = 140,\n  className,\n  children,\n  onPointerDown,\n  ...props\n}: FloatingWindowProps) {\n  // Controlled when position/size are given; internal state otherwise.\n  const [internalPosition, setInternalPosition] =\n    React.useState<Position>(defaultPosition);\n  const [internalSize, setInternalSize] = React.useState<Size>(defaultSize);\n  const position = positionProp ?? internalPosition;\n  const size = sizeProp ?? internalSize;\n  const [dragging, setDragging] = React.useState(false);\n  const drag = React.useRef<DragState | null>(null);\n\n  function updatePosition(next: Position) {\n    if (onPositionChange) onPositionChange(next);\n    else setInternalPosition(next);\n  }\n\n  function updateSize(next: Size) {\n    if (onSizeChange) onSizeChange(next);\n    else setInternalSize(next);\n  }\n\n  function beginDrag(edge: DragState[\"edge\"], event: React.PointerEvent) {\n    event.preventDefault();\n    drag.current = {\n      edge,\n      startX: event.clientX,\n      startY: event.clientY,\n      originPosition: position,\n      originSize: size,\n    };\n    setDragging(true);\n    // Capture the pointer so move/up events keep firing over this window\n    // even when the cursor leaves it.\n    event.currentTarget.setPointerCapture(event.pointerId);\n  }\n\n  function onRootPointerDown(event: React.PointerEvent<HTMLDivElement>) {\n    if (dragModifier && event[modifierToEventKey(dragModifier)]) {\n      beginDrag(\"move\", event);\n    }\n    onPointerDown?.(event);\n  }\n\n  function onPointerMove(event: React.PointerEvent) {\n    const state = drag.current;\n    if (!state) return;\n    const dx = event.clientX - state.startX;\n    const dy = event.clientY - state.startY;\n\n    if (state.edge === \"move\") {\n      updatePosition({\n        x: state.originPosition.x + dx,\n        y: state.originPosition.y + dy,\n      });\n      return;\n    }\n\n    let { x, y } = state.originPosition;\n    let { width, height } = state.originSize;\n\n    // Grow/shrink from the dragged edge, clamping to the minimum size.\n    // Edges that pull the opposite side also translate the window so the\n    // anchored edge stays put.\n    if (state.edge.includes(\"e\")) {\n      width = Math.max(minWidth, state.originSize.width + dx);\n    }\n    if (state.edge.includes(\"s\")) {\n      height = Math.max(minHeight, state.originSize.height + dy);\n    }\n    if (state.edge.includes(\"w\")) {\n      width = Math.max(minWidth, state.originSize.width - dx);\n      x = state.originPosition.x + (state.originSize.width - width);\n    }\n    if (state.edge.includes(\"n\")) {\n      height = Math.max(minHeight, state.originSize.height - dy);\n      y = state.originPosition.y + (state.originSize.height - height);\n    }\n\n    updatePosition({ x, y });\n    updateSize({ width, height });\n  }\n\n  function endDrag() {\n    drag.current = null;\n    setDragging(false);\n  }\n\n  return (\n    <div\n      style={{\n        left: position.x,\n        top: position.y,\n        width: size.width,\n        height: size.height,\n        zIndex,\n      }}\n      className={cn(\n        \"absolute flex flex-col overflow-hidden rounded-lg border bg-background shadow-lg\",\n        dragging && \"select-none\",\n        className,\n      )}\n      onPointerDown={onRootPointerDown}\n      onPointerMove={onPointerMove}\n      onPointerUp={endDrag}\n      onPointerCancel={endDrag}\n      {...props}\n    >\n      {chrome ? (\n        <div\n          onPointerDown={(event) => beginDrag(\"move\", event)}\n          className=\"flex h-9 shrink-0 touch-none items-center justify-between border-b bg-muted/50 px-3 active:cursor-grabbing\"\n        >\n          <span className=\"cursor-grab text-sm font-medium\">{title}</span>\n          {onClose ? (\n            <button\n              type=\"button\"\n              aria-label={`Close ${title ?? \"window\"}`}\n              onClick={onClose}\n              onPointerDown={(event) => event.stopPropagation()}\n              className=\"flex size-4 items-center justify-center rounded-sm text-muted-foreground/70 hover:bg-accent hover:text-foreground\"\n            >\n              ×\n            </button>\n          ) : (\n            <span aria-hidden className=\"flex gap-1\">\n              <i className=\"size-1.5 rounded-full bg-muted-foreground/40\" />\n              <i className=\"size-1.5 rounded-full bg-muted-foreground/40\" />\n              <i className=\"size-1.5 rounded-full bg-muted-foreground/40\" />\n            </span>\n          )}\n        </div>\n      ) : null}\n      <div className={cn(\"flex-1 overflow-auto p-4\", contentClassName)}>\n        {children}\n      </div>\n      {resizeHandles.map((handle) => (\n        <div\n          key={handle.edge}\n          onPointerDown={(event) => beginDrag(handle.edge, event)}\n          className={cn(\"absolute touch-none\", handle.className)}\n        />\n      ))}\n    </div>\n  );\n}\n\nexport { FloatingWindow };\n"}], Component: resolveExport(FloatingWindowDemoModule, "FloatingWindowDemo") },
  { name: "workspace-demo", title: "Workspace Demo", description: "IDE-style tiled workspace: views, tab stacks, splits, floating windows, keymap, and a state inspector.", files: [{"path":"registry/blocks/workspace/workspace-demo.tsx","source":"/**\n * Workspace demo — functional test bed (segments 1–3).\n *\n * The views are deliberately real enough to prove the system: their state\n * lives in useViewWorkingState (keyed by view id), so an editor keeps its text and\n * a calculator keeps its display when the view floats, docks, splits, or\n * moves between tabs.\n */\n\nimport * as React from \"react\";\n\nimport { GripVertical } from \"lucide-react\";\n\nimport { Button } from \"@/registry/ui/button\";\nimport { WorkspaceProvider } from \"@/registry/hooks/use-workspace\";\nimport {\n  useWorkspaceConfig,\n  useWorkspaceDispatch,\n  useWorkspaceState,\n  useWorkspaceStore,\n  type WorkspaceViewProps,\n} from \"@/registry/hooks/use-workspace\";\nimport { useViewWorkingState } from \"@/registry/hooks/use-view-working-state\";\nimport { requestPopout } from \"@/registry/blocks/workspace/workspace-popouts\";\nimport {\n  deserializeWorkspaceState,\n  serializeWorkspaceState,\n} from \"@/registry/lib/workspace/persistence\";\nimport { WorkspaceTiled } from \"@/registry/blocks/workspace/workspace-tiled\";\nimport { WorkspaceFloating } from \"@/registry/blocks/workspace/workspace-floating\";\nimport { createInitialState } from \"@/registry/lib/workspace/factory\";\nimport type { ModifierKey } from \"@/registry/lib/workspace/keymap\";\n\nconst initialLayout = createInitialState({\n  direction: \"horizontal\",\n  ratio: 0.6,\n  first: {\n    views: [{ type: \"editor\", title: \"Editor\" }],\n  },\n  second: {\n    direction: \"vertical\",\n    ratio: 0.55,\n    first: {\n      views: [\n        { type: \"calculator\", title: \"Calculator\" },\n        { type: \"clock\", title: \"Clock\" },\n      ],\n    },\n    second: { views: [{ type: \"files\", title: \"Files\" }] },\n  },\n});\n\nconst demoViews = {\n  editor: EditorView,\n  calculator: CalculatorView,\n  files: FilesView,\n  clock: ClockView,\n};\n\nexport function WorkspaceDemo() {\n  const [chrome, setChrome] = React.useState<\"frame\" | \"frameless\">(\"frame\");\n  const [dragModifier, setDragModifier] = React.useState<ModifierKey>(\"ctrl\");\n  const [panel, setPanel] = React.useState<\"none\" | \"keys\" | \"state\">(\"none\");\n\n  return (\n    <div className=\"absolute inset-0\">\n      <WorkspaceProvider\n        views={demoViews}\n        initialState={initialLayout}\n        config={{ floating: { chrome, dragModifier } }}\n      >\n        <FloatingToolbar\n          actions={<ToolbarActions />}\n          secondary={\n            <>\n              <ConfigControls\n                chrome={chrome}\n                onChromeChange={setChrome}\n                dragModifier={dragModifier}\n                onDragModifierChange={setDragModifier}\n              />\n              <Button\n                size=\"sm\"\n                variant={panel === \"keys\" ? \"secondary\" : \"ghost\"}\n                onClick={() => setPanel(panel === \"keys\" ? \"none\" : \"keys\")}\n              >\n                Keys\n              </Button>\n              <Button\n                size=\"sm\"\n                variant={panel === \"state\" ? \"secondary\" : \"ghost\"}\n                onClick={() => setPanel(panel === \"state\" ? \"none\" : \"state\")}\n              >\n                State\n              </Button>\n              <PersistenceButtons />\n            </>\n          }\n        />\n        {panel === \"keys\" ? <KeysPanel /> : null}\n        {panel === \"state\" ? <StateInspector /> : null}\n        <WorkspaceTiled />\n        <WorkspaceFloating />\n        <StatusBar />\n      </WorkspaceProvider>\n    </div>\n  );\n}\n\n// ---------------------------------------------------------------------------\n// Toolbar / panels / status bar\n// ---------------------------------------------------------------------------\n\n/**\n * Compact floating toolbar: two rows, draggable by its grip. Starts centered\n * at the top (measured after first paint), then lives wherever you drop it.\n */\nfunction FloatingToolbar({\n  actions,\n  secondary,\n}: {\n  actions: React.ReactNode;\n  secondary: React.ReactNode;\n}) {\n  const containerRef = React.useRef<HTMLDivElement | null>(null);\n  const barRef = React.useRef<HTMLDivElement | null>(null);\n  const [position, setPosition] = React.useState<{\n    x: number;\n    y: number;\n  } | null>(null);\n  const drag = React.useRef<{\n    startX: number;\n    startY: number;\n    originX: number;\n    originY: number;\n  } | null>(null);\n\n  // Measure once so the bar starts centered without knowing its own width.\n  React.useLayoutEffect(() => {\n    if (position || !containerRef.current || !barRef.current) return;\n    const rect = containerRef.current.getBoundingClientRect();\n    setPosition({\n      x: Math.max(8, rect.width / 2 - barRef.current.offsetWidth / 2),\n      y: 10,\n    });\n  }, [position]);\n\n  function onGripPointerDown(event: React.PointerEvent<HTMLDivElement>) {\n    if (!position) return;\n    event.preventDefault();\n    drag.current = {\n      startX: event.clientX,\n      startY: event.clientY,\n      originX: position.x,\n      originY: position.y,\n    };\n    event.currentTarget.setPointerCapture(event.pointerId);\n  }\n\n  function onGripPointerMove(event: React.PointerEvent<HTMLDivElement>) {\n    const state = drag.current;\n    if (!state) return;\n    setPosition({\n      x: state.originX + event.clientX - state.startX,\n      y: state.originY + event.clientY - state.startY,\n    });\n  }\n\n  function onGripPointerUp() {\n    drag.current = null;\n  }\n\n  return (\n    <div ref={containerRef} className=\"pointer-events-none absolute inset-0\">\n      <div\n        ref={barRef}\n        style={\n          position\n            ? { left: position.x, top: position.y }\n            : { visibility: \"hidden\" }\n        }\n        className=\"pointer-events-auto absolute z-[9999] flex items-center gap-1 rounded-md border bg-background/95 p-1 shadow-md\"\n      >\n        <div\n          role=\"button\"\n          aria-label=\"Move toolbar\"\n          tabIndex={0}\n          onPointerDown={onGripPointerDown}\n          onPointerMove={onGripPointerMove}\n          onPointerUp={onGripPointerUp}\n          onPointerCancel={onGripPointerUp}\n          className=\"flex h-full cursor-grab touch-none items-center rounded text-muted-foreground/70 hover:bg-accent hover:text-foreground active:cursor-grabbing\"\n        >\n          <GripVertical className=\"size-3.5\" />\n        </div>\n        <div className=\"flex flex-col items-start gap-1\">\n          <div className=\"flex gap-1\">{actions}</div>\n          <div className=\"flex gap-1\">{secondary}</div>\n        </div>\n      </div>\n    </div>\n  );\n}\n\nfunction ToolbarActions() {\n  const dispatch = useWorkspaceDispatch();\n  const store = useWorkspaceStore();\n  const state = useWorkspaceState();\n\n  function popoutActive() {\n    const viewId = state.activeStackId\n      ? state.stacks[state.activeStackId]?.activeViewId\n      : undefined;\n    if (viewId) requestPopout(store, viewId);\n  }\n\n  return (\n    <>\n      <Button\n        size=\"sm\"\n        variant=\"ghost\"\n        onClick={() =>\n          dispatch({\n            type: \"view/open\",\n            viewType: \"editor\",\n            title: `Editor ${new Date().toLocaleTimeString()}`,\n          })\n        }\n      >\n        Open Editor\n      </Button>\n      <Button\n        size=\"sm\"\n        variant=\"ghost\"\n        onClick={() =>\n          dispatch({ type: \"view/open\", viewType: \"files\", title: \"Files\" })\n        }\n      >\n        Open Files\n      </Button>\n      <Button\n        size=\"sm\"\n        variant=\"outline\"\n        onClick={() =>\n          dispatch({\n            type: \"stack/split\",\n            direction: \"horizontal\",\n            viewType: \"editor\",\n            title: \"Editor\",\n          })\n        }\n      >\n        Split Right\n      </Button>\n      <Button\n        size=\"sm\"\n        variant=\"ghost\"\n        onClick={() => store.applyCommand(\"view/float\")}\n      >\n        Float\n      </Button>\n      <Button\n        size=\"sm\"\n        variant=\"ghost\"\n        onClick={() => store.applyCommand(\"view/dock\")}\n      >\n        Dock\n      </Button>\n      <Button size=\"sm\" variant=\"ghost\" onClick={popoutActive}>\n        Popout\n      </Button>\n    </>\n  );\n}\n\nconst STORAGE_KEY = \"edcn-workspace-demo\";\n\nfunction PersistenceButtons() {\n  const state = useWorkspaceState();\n  const store = useWorkspaceStore();\n\n  return (\n    <>\n      <Button\n        size=\"sm\"\n        variant=\"ghost\"\n        onClick={() =>\n          localStorage.setItem(STORAGE_KEY, serializeWorkspaceState(state))\n        }\n      >\n        Save\n      </Button>\n      <Button\n        size=\"sm\"\n        variant=\"ghost\"\n        onClick={() => {\n          const json = localStorage.getItem(STORAGE_KEY);\n          if (!json) return;\n          const restored = deserializeWorkspaceState(json);\n          if (restored) store.hydrate(restored);\n        }}\n      >\n        Load\n      </Button>\n      <Button\n        size=\"sm\"\n        variant=\"ghost\"\n        onClick={() => {\n          localStorage.removeItem(STORAGE_KEY);\n          store.hydrate(initialLayout);\n        }}\n      >\n        Reset\n      </Button>\n    </>\n  );\n}\n\nfunction ConfigControls({\n  chrome,\n  onChromeChange,\n  dragModifier,\n  onDragModifierChange,\n}: {\n  chrome: \"frame\" | \"frameless\";\n  onChromeChange: (chrome: \"frame\" | \"frameless\") => void;\n  dragModifier: ModifierKey;\n  onDragModifierChange: (modifier: ModifierKey) => void;\n}) {\n  return (\n    <>\n      <Button\n        size=\"sm\"\n        variant={chrome === \"frame\" ? \"secondary\" : \"ghost\"}\n        onClick={() =>\n          onChromeChange(chrome === \"frame\" ? \"frameless\" : \"frame\")\n        }\n      >\n        {chrome === \"frame\" ? \"Chrome: frame\" : \"Chrome: frameless\"}\n      </Button>\n      <Button\n        size=\"sm\"\n        variant=\"ghost\"\n        onClick={() =>\n          onDragModifierChange(dragModifier === \"ctrl\" ? \"alt\" : \"ctrl\")\n        }\n      >\n        Drag: {dragModifier}\n      </Button>\n    </>\n  );\n}\n\nfunction KeysPanel() {\n  const config = useWorkspaceConfig();\n  return (\n    <div className=\"absolute top-12 left-1/2 z-[9999] w-96 -translate-x-1/2 rounded-md border bg-background p-3 text-xs shadow-md\">\n      <p className=\"mb-2 font-medium\">Keyboard (override via config.keymap)</p>\n      <ul className=\"space-y-1 font-mono text-muted-foreground\">\n        <li>alt+w — close active view</li>\n        <li>alt+ArrowDown / alt+ArrowUp — next / previous tab</li>\n        <li>alt+f — float active view</li>\n        <li>alt+d — dock active view</li>\n        <li>alt+p — popout active view into its own browser window</li>\n      </ul>\n      <p className=\"mt-3 mb-2 font-medium\">Pointer</p>\n      <ul className=\"space-y-1 font-mono text-muted-foreground\">\n        <li>\n          drag divider — resize panes · double-click — reset · arrows — nudge\n        </li>\n        <li>\n          {config.floating.chrome === \"frameless\"\n            ? `hold ${config.floating.dragModifier} + drag — move window from anywhere`\n            : \"drag title bar to move · ctrl+drag also works anywhere\"}\n        </li>\n        <li>click a floating window — bring to front</li>\n      </ul>\n      <p className=\"mt-3 text-muted-foreground\">\n        Tip: click a tab strip to focus a stack — Open/Split target it.\n      </p>\n    </div>\n  );\n}\n\nfunction StateInspector() {\n  const state = useWorkspaceState();\n  return (\n    <pre className=\"absolute top-12 left-1/2 z-[9999] max-h-72 w-[28rem] -translate-x-1/2 overflow-auto rounded-md border bg-background p-3 text-left text-xs shadow-md\">\n      {JSON.stringify(state, null, 2)}\n    </pre>\n  );\n}\n\nfunction StatusBar() {\n  const state = useWorkspaceState();\n  const activeStack = state.activeStackId\n    ? state.stacks[state.activeStackId]\n    : undefined;\n  const activeView = activeStack?.activeViewId\n    ? state.views[activeStack.activeViewId]\n    : undefined;\n\n  return (\n    <div className=\"absolute inset-x-0 bottom-0 z-[9999] flex items-center justify-between border-t bg-background/95 px-3 py-1 text-xs text-muted-foreground\">\n      <span>\n        Active view:{\" \"}\n        <span className=\"font-mono text-foreground\">\n          {activeView ? `${activeView.title} · ${activeView.id}` : \"none\"}\n        </span>\n      </span>\n      <span>\n        stacks {Object.keys(state.stacks).length} · views{\" \"}\n        {Object.keys(state.views).length} · floating{\" \"}\n        {Object.keys(state.floating).length}\n      </span>\n    </div>\n  );\n}\n\n/** Shows the view's stable id — the same instance follows its window around. */\nfunction ViewBadge({ view }: { view: { id: string } }) {\n  return (\n    <span className=\"rounded bg-muted px-1.5 py-0.5 font-mono text-[10px] text-muted-foreground\">\n      {view.id}\n    </span>\n  );\n}\n\n// ---------------------------------------------------------------------------\n// Demo views — each keeps working state in useViewWorkingState, keyed by view id,\n// so state survives float / dock / split / tab moves.\n// ---------------------------------------------------------------------------\n\nfunction EditorView({ view }: WorkspaceViewProps) {\n  const [text, setText] = useViewWorkingState(\n    view.id,\n    () => `# ${view.title}\\n\\nType here, then Float or Dock me.\\n`,\n  );\n  const words = text.trim() ? text.trim().split(/\\s+/).length : 0;\n\n  return (\n    <div className=\"flex h-full flex-col\">\n      <div className=\"flex items-center justify-between border-b px-2 py-1\">\n        <ViewBadge view={view} />\n        <span className=\"text-[10px] text-muted-foreground\">\n          {text.split(\"\\n\").length} lines · {words} words\n        </span>\n      </div>\n      <textarea\n        value={text}\n        onChange={(event) => setText(event.target.value)}\n        className=\"h-full w-full resize-none bg-transparent p-3 font-mono text-sm outline-none\"\n        spellCheck={false}\n      />\n    </div>\n  );\n}\n\ninterface CalcState {\n  display: string;\n  acc: number | null;\n  op: string | null;\n  fresh: boolean;\n}\n\nconst CALC_KEYS = [\n  [\"7\", \"8\", \"9\", \"/\"],\n  [\"4\", \"5\", \"6\", \"*\"],\n  [\"1\", \"2\", \"3\", \"-\"],\n  [\"0\", \".\", \"=\", \"+\"],\n] as const;\n\nfunction applyOp(a: number, b: number, op: string): number {\n  switch (op) {\n    case \"+\":\n      return a + b;\n    case \"-\":\n      return a - b;\n    case \"*\":\n      return a * b;\n    case \"/\":\n      return b === 0 ? NaN : a / b;\n    default:\n      return b;\n  }\n}\n\nfunction calcPress(state: CalcState, key: string): CalcState {\n  if (key === \"C\") return { display: \"0\", acc: null, op: null, fresh: true };\n  if (key === \"=\") {\n    if (state.op !== null && state.acc !== null) {\n      return {\n        display: String(applyOp(state.acc, Number(state.display), state.op)),\n        acc: null,\n        op: null,\n        fresh: true,\n      };\n    }\n    return state;\n  }\n  if ([\"+\", \"-\", \"*\", \"/\"].includes(key)) {\n    const current = Number(state.display);\n    const acc =\n      state.op !== null && state.acc !== null && !state.fresh\n        ? applyOp(state.acc, current, state.op)\n        : current;\n    return { display: String(acc), acc, op: key, fresh: true };\n  }\n  if (state.fresh) {\n    return { ...state, display: key === \".\" ? \"0.\" : key, fresh: false };\n  }\n  if (key === \".\" && state.display.includes(\".\")) return state;\n  return {\n    ...state,\n    display: state.display === \"0\" && key !== \".\" ? key : state.display + key,\n  };\n}\n\nconst INITIAL_CALC: CalcState = {\n  display: \"0\",\n  acc: null,\n  op: null,\n  fresh: true,\n};\n\nfunction CalculatorView({ view }: WorkspaceViewProps) {\n  const [calc, setCalc] = useViewWorkingState(\n    `calc:${view.id}`,\n    () => INITIAL_CALC,\n  );\n\n  return (\n    <div className=\"flex h-full flex-col\">\n      <div className=\"flex items-center justify-between border-b px-2 py-1\">\n        <ViewBadge view={view} />\n        {calc.op !== null ? (\n          <span className=\"text-[10px] text-muted-foreground\">\n            pending {calc.op} {calc.acc}\n          </span>\n        ) : null}\n      </div>\n      <div className=\"px-3 py-2 text-right font-mono text-2xl tabular-nums\">\n        {calc.display}\n      </div>\n      <div className=\"grid flex-1 grid-cols-4 gap-1 p-2\">\n        {CALC_KEYS.flat().map((key) => (\n          <Button\n            key={key}\n            variant={\n              [\"/\", \"*\", \"-\", \"+\", \"=\"].includes(key) ? \"secondary\" : \"outline\"\n            }\n            size=\"sm\"\n            className=\"font-mono\"\n            onClick={() => setCalc((prev) => calcPress(prev, key))}\n          >\n            {key}\n          </Button>\n        ))}\n        <Button\n          variant=\"ghost\"\n          size=\"sm\"\n          className=\"col-span-4 font-mono text-destructive\"\n          onClick={() => setCalc(INITIAL_CALC)}\n        >\n          C\n        </Button>\n      </div>\n    </div>\n  );\n}\n\nconst DEMO_FILES = [\n  \"src/main.tsx\",\n  \"src/registry-preview.tsx\",\n  \"registry/lib/workspace/reducer.ts\",\n  \"registry/lib/workspace/types.ts\",\n  \"package.json\",\n];\n\nfunction FilesView({ view }: WorkspaceViewProps) {\n  const [selected, setSelected] = useViewWorkingState<string | null>(\n    `files:${view.id}`,\n    () => null,\n  );\n  const dispatch = useWorkspaceDispatch();\n\n  return (\n    <div className=\"flex h-full flex-col\">\n      <div className=\"flex items-center justify-between border-b px-2 py-1\">\n        <ViewBadge view={view} />\n        <span className=\"text-[10px] text-muted-foreground\">\n          click a file to open it\n        </span>\n      </div>\n      <ul className=\"flex-1 overflow-auto p-1 text-sm\">\n        {DEMO_FILES.map((file) => (\n          <li key={file}>\n            <button\n              type=\"button\"\n              onClick={() => {\n                setSelected(file);\n                // Opens into the ACTIVE stack — click a tab strip first to\n                // choose the target.\n                dispatch({\n                  type: \"view/open\",\n                  viewType: \"editor\",\n                  title: file,\n                });\n              }}\n              className={\n                \"w-full truncate rounded px-2 py-1 text-left font-mono text-xs \" +\n                (selected === file\n                  ? \"bg-accent text-accent-foreground\"\n                  : \"text-muted-foreground hover:bg-accent/60 hover:text-foreground\")\n              }\n            >\n              {file}\n            </button>\n          </li>\n        ))}\n      </ul>\n    </div>\n  );\n}\n\nfunction ClockView({ view }: WorkspaceViewProps) {\n  const [now, setNow] = React.useState(() => new Date());\n  React.useEffect(() => {\n    const timer = setInterval(() => setNow(new Date()), 1000);\n    return () => clearInterval(timer);\n  }, []);\n\n  return (\n    <div className=\"flex h-full flex-col items-center justify-center gap-2\">\n      <span className=\"text-3xl font-bold tabular-nums\">\n        {now.toLocaleTimeString()}\n      </span>\n      <ViewBadge view={view} />\n    </div>\n  );\n}\n"},{"path":"registry/blocks/workspace/workspace.tsx","source":"/**\n * <Workspace> — the full workspace system block.\n *\n * Owns the store (via WorkspaceProvider), resolves the consumer's views map\n * and config, mounts the keymap listener, and renders the surfaces: the\n * tiled tree, floating windows, and popout browser windows.\n *\n * ```tsx\n * <Workspace\n *   views={{ editor: EditorView, calculator: CalculatorView }}\n *   initialState={createInitialState({ ... })}\n *   config={{ floating: { chrome: \"frameless\", dragModifier: \"ctrl\" } }}\n * />\n * ```\n */\n\nimport { WorkspaceProvider } from \"@/registry/hooks/use-workspace\";\nimport type { WorkspaceViewsMap } from \"@/registry/hooks/use-workspace\";\nimport { useWorkspaceKeymap } from \"@/registry/hooks/use-workspace-keymap\";\nimport { WorkspaceFloating } from \"@/registry/blocks/workspace/workspace-floating\";\nimport { WorkspacePopouts } from \"@/registry/blocks/workspace/workspace-popouts\";\nimport { WorkspaceTiled } from \"@/registry/blocks/workspace/workspace-tiled\";\nimport type { WorkspaceConfigInput } from \"@/registry/lib/workspace/config\";\nimport type { WorkspaceState } from \"@/registry/lib/workspace/types\";\n\nexport interface WorkspaceProps {\n   views: WorkspaceViewsMap;\n   initialState: WorkspaceState;\n   config?: WorkspaceConfigInput;\n   className?: string;\n}\n\nexport function Workspace({\n   views,\n   initialState,\n   config,\n   className,\n}: WorkspaceProps) {\n   return (\n      <div className={className ?? \"absolute inset-0\"}>\n         <WorkspaceProvider\n            views={views}\n            initialState={initialState}\n            config={config}\n         >\n            <WorkspaceKeymapListener />\n            <WorkspaceTiled />\n            <WorkspaceFloating />\n            <WorkspacePopouts />\n         </WorkspaceProvider>\n      </div>\n   );\n}\n\n/** Hook host: useWorkspaceKeymap registers a window-level listener. */\nfunction WorkspaceKeymapListener() {\n   useWorkspaceKeymap();\n   return null;\n}\n"},{"path":"registry/blocks/workspace/workspace-tiled.tsx","source":"/**\n * The main tiled surface: renders the workspace's tiled LayoutNode tree inside a\n * padded container. Floating/popout surfaces (segment 3+) will render\n * alongside this in <Workspace>.\n */\n\nimport { useWorkspaceState } from \"@/registry/hooks/use-workspace\";\nimport { WorkspaceLayout } from \"@/registry/ui/workspace-layout\";\n\nexport function WorkspaceTiled() {\n return (\n  <div className=\"relative h-full w-full p-1.5\">\n   <TiledRoot />\n  </div>\n );\n}\n\n/** Subscribes to the store; re-renders only the recursive layout tree. */\nfunction TiledRoot() {\n const state = useWorkspaceState();\n return <WorkspaceLayout node={state.tiled} />;\n}\n"},{"path":"registry/blocks/workspace/workspace-floating.tsx","source":"/**\n * Floating surfaces layer: renders every floating window over the tiled\n * area. Geometry is owned by workspace state; each window hosts the same\n * placement-agnostic WorkspaceStack used in the tiled tree.\n */\n\nimport { FloatingWindow } from \"@/registry/ui/floating-window\";\nimport { WorkspaceStack } from \"@/registry/ui/workspace-stack\";\nimport {\n  useWorkspaceConfig,\n  useWorkspaceDispatch,\n  useWorkspaceState,\n} from \"@/registry/hooks/use-workspace\";\n\nexport function WorkspaceFloating() {\n  const state = useWorkspaceState();\n  const config = useWorkspaceConfig();\n  const dispatch = useWorkspaceDispatch();\n\n  const surfaces = Object.values(state.floating);\n  if (surfaces.length === 0) return null;\n\n  return (\n    <>\n      {surfaces.map((surface) => {\n        const stack = state.stacks[surface.stackId];\n        const activeView = stack?.activeViewId\n          ? state.views[stack.activeViewId]\n          : undefined;\n\n        return (\n          <FloatingWindow\n            key={surface.id}\n            position={{ x: surface.x, y: surface.y }}\n            size={{ width: surface.width, height: surface.height }}\n            onPositionChange={(position) =>\n              dispatch({\n                type: \"floating/move\",\n                surfaceId: surface.id,\n                x: position.x,\n                y: position.y,\n              })\n            }\n            onSizeChange={(size) =>\n              dispatch({\n                type: \"floating/resize\",\n                surfaceId: surface.id,\n                width: size.width,\n                height: size.height,\n              })\n            }\n            chrome={config.floating.chrome === \"frame\"}\n            dragModifier={config.floating.dragModifier}\n            zIndex={surface.zIndex}\n            title={activeView?.title ?? \"Window\"}\n            onClose={() =>\n              dispatch({ type: \"floating/close\", surfaceId: surface.id })\n            }\n            onPointerDown={() =>\n              dispatch({ type: \"floating/focus\", surfaceId: surface.id })\n            }\n            contentClassName=\"p-0\"\n          >\n            <WorkspaceStack\n              stackId={surface.stackId}\n              className=\"h-full w-full rounded-none border-0\"\n            />\n          </FloatingWindow>\n        );\n      })}\n    </>\n  );\n}\n"},{"path":"registry/blocks/workspace/workspace-popouts.tsx","source":"/**\n * Popout surfaces layer — hosts stacks in real browser windows.\n *\n * State owns only serializable facts (PopoutSurface); the live Window\n * handles live in this module. A window is opened synchronously inside the\n * user gesture via requestPopout() (popup blockers), then adopted here; the\n * reconcile effect also opens windows for restored/programmatic surfaces\n * and rolls surfaces back if the browser refuses.\n *\n * The child document gets a second React root rendering a WorkspaceProvider\n * bound to the SAME store instance, so a popout is a live viewport onto the\n * one workspace state — closing it (or its last tab) just edits state.\n *\n * Note: HTML5 drag & drop cannot cross browser windows, so dragging tabs\n * between the main window and popouts is out of scope by design.\n */\n\nimport * as React from \"react\";\nimport { createRoot, type Root } from \"react-dom/client\";\n\nimport { createId } from \"@/registry/lib/workspace/ids\";\nimport {\n  DEFAULT_POPOUT_HEIGHT,\n  DEFAULT_POPOUT_WIDTH,\n} from \"@/registry/lib/workspace/model/surface\";\nimport type { WorkspaceStore } from \"@/registry/lib/workspace/store\";\nimport {\n  WorkspaceProvider,\n  useWorkspaceContext,\n  useWorkspaceDispatch,\n  useWorkspaceState,\n} from \"@/registry/hooks/use-workspace\";\nimport { WorkspaceStack } from \"@/registry/ui/workspace-stack\";\n\ninterface LivePopout {\n  win: Window;\n  root: Root;\n}\n\n/** Windows opened inside the user gesture, awaiting their surface record. */\nconst pendingWindows = new Map<string, Window>();\n/** surfaceId -> live window + React root. */\nconst livePopouts = new Map<string, LivePopout>();\n\n/**\n * Pop out a view into its own browser window. MUST be called from a user\n * event handler (click) so window.open is not blocked; the window is opened\n * synchronously, then the surface is committed to state.\n */\nexport function requestPopout(store: WorkspaceStore, viewId: string): void {\n  const state = store.getState();\n  const view = state.views[viewId];\n  if (!view) return;\n\n  const surfaceId = createId(\"popout\");\n  const win = window.open(\n    \"\",\n    `edcn-popout-${surfaceId}`,\n    `popup,width=${DEFAULT_POPOUT_WIDTH},height=${DEFAULT_POPOUT_HEIGHT}`,\n  );\n  if (!win) return; // popup blocked — nothing is committed to state\n\n  pendingWindows.set(surfaceId, win);\n  store.dispatch({\n    type: \"view/popout\",\n    surfaceId,\n    viewId,\n    width: DEFAULT_POPOUT_WIDTH,\n    height: DEFAULT_POPOUT_HEIGHT,\n  });\n}\n\n/** Clone the host document's styles into a popout document. */\nfunction copyStyles(targetDoc: Document): void {\n  for (const el of Array.from(\n    document.head.querySelectorAll(\"style, link[rel=stylesheet]\"),\n  )) {\n    targetDoc.head.appendChild(el.cloneNode(true));\n  }\n}\n\n/**\n * Reconciles state.popouts with live browser windows. Renders nothing in\n * the main window; its effect owns the popout lifecycle.\n */\nexport function WorkspacePopouts(): null {\n  const { store, views, config } = useWorkspaceContext();\n  const state = useWorkspaceState();\n  const dispatch = useWorkspaceDispatch();\n  // Only the set of surface ids should re-run the reconcile effect.\n  const surfaceIds = Object.keys(state.popouts).join(\",\");\n\n  React.useEffect(() => {\n    // Close windows whose surface was removed from state (last tab closed,\n    // docked, or popout/close).\n    for (const [surfaceId, entry] of Array.from(livePopouts)) {\n      if (!state.popouts[surfaceId]) {\n        entry.root.unmount();\n        if (!entry.win.closed) entry.win.close();\n        livePopouts.delete(surfaceId);\n      }\n    }\n\n    // Open windows for surfaces that don't have one yet.\n    for (const surface of Object.values(state.popouts)) {\n      if (livePopouts.has(surface.id)) continue;\n\n      const win =\n        pendingWindows.get(surface.id) ??\n        window.open(\n          \"\",\n          `edcn-popout-${surface.id}`,\n          `popup,width=${surface.width},height=${surface.height}`,\n        );\n      pendingWindows.delete(surface.id);\n\n      if (!win) {\n        // Browser refused (programmatic open outside a gesture) — roll the\n        // surface back so state never references a window that can't exist.\n        dispatch({ type: \"popout/close\", surfaceId: surface.id });\n        continue;\n      }\n\n      const doc = win.document;\n      doc.title = \"Workspace popout\";\n      copyStyles(doc);\n      const container = doc.createElement(\"div\");\n      container.style.cssText = \"position:fixed;inset:0;\";\n      doc.body.appendChild(container);\n\n      const root = createRoot(container);\n      root.render(\n        React.createElement(\n          WorkspaceProvider,\n          { store, views, config },\n          React.createElement(WorkspaceStack, {\n            stackId: surface.stackId,\n            className: \"h-full w-full rounded-none border-0\",\n          }),\n        ),\n      );\n\n      // Closing the OS window closes the surface (and its views).\n      win.addEventListener(\"beforeunload\", () => {\n        dispatch({ type: \"popout/close\", surfaceId: surface.id });\n      });\n\n      livePopouts.set(surface.id, { win, root });\n    }\n    // surfaceIds keeps this effect from re-running on unrelated state edits.\n  }, [surfaceIds, state.popouts, store, views, config, dispatch]);\n\n  // Close every popout when the host unmounts.\n  React.useEffect(() => {\n    const entries = Array.from(livePopouts.values());\n    return () => {\n      for (const entry of entries) {\n        entry.root.unmount();\n        if (!entry.win.closed) entry.win.close();\n      }\n      livePopouts.clear();\n    };\n  }, []);\n\n  return null;\n}\n"},{"path":"registry/ui/workspace-tab-list.tsx","source":"/**\n * Tab strip for a single stack. Fully placement-agnostic: it reads its stack\n * from workspace state and works the same inside a split, a floating window,\n * or a popout.\n */\n\nimport * as React from \"react\";\n\nimport { cn } from \"@/lib/utils\";\nimport {\n  useWorkspaceDispatch,\n  useWorkspaceState,\n} from \"@/registry/hooks/use-workspace\";\nimport { useWorkspaceDrag } from \"@/registry/hooks/use-workspace-drag\";\n\nexport interface WorkspaceTabListProps\n  extends React.HTMLAttributes<HTMLDivElement> {\n  stackId: string;\n}\n\nexport function WorkspaceTabList({\n  stackId,\n  className,\n  ...props\n}: WorkspaceTabListProps) {\n  const state = useWorkspaceState();\n  const dispatch = useWorkspaceDispatch();\n  const dragContext = useWorkspaceDrag();\n\n  const stack = state.stacks[stackId];\n  if (!stack) return null;\n\n  function startDrag(event: React.DragEvent<HTMLDivElement>, viewId: string) {\n    if (!dragContext) return;\n    dragContext.beginDrag(\n      { viewId, sourceStackId: stackId },\n      event.dataTransfer,\n    );\n  }\n\n  return (\n    <div\n      role=\"tablist\"\n      className={cn(\n        \"flex h-9 items-stretch gap-0.5 overflow-x-auto border-b bg-muted/40 px-1\",\n        className,\n      )}\n      {...props}\n    >\n      {stack.viewIds.map((viewId) => {\n        const view = state.views[viewId];\n        if (!view) return null;\n        const active = stack.activeViewId === viewId;\n        return (\n          <div\n            key={viewId}\n            role=\"tab\"\n            aria-selected={active}\n            tabIndex={0}\n            draggable\n            onDragStart={(event) => startDrag(event, viewId)}\n            onDragEnd={() => dragContext?.endDrag()}\n            onClick={() => dispatch({ type: \"view/activate\", viewId })}\n            onKeyDown={(event) => {\n              if (event.key === \"Enter\" || event.key === \" \") {\n                dispatch({ type: \"view/activate\", viewId });\n              }\n            }}\n            className={cn(\n              \"group flex max-w-48 shrink-0 cursor-pointer items-center gap-1.5 rounded-md px-2.5 text-sm outline-none select-none\",\n              \"focus-visible:ring-ring/50 focus-visible:ring-2\",\n              active\n                ? \"bg-background text-foreground shadow-xs\"\n                : \"text-muted-foreground hover:bg-accent/60 hover:text-foreground\",\n            )}\n          >\n            <span className=\"truncate\">{view.title}</span>\n            <button\n              type=\"button\"\n              aria-label={`Close ${view.title}`}\n              onClick={(event) => {\n                event.stopPropagation();\n                dispatch({ type: \"view/close\", viewId });\n              }}\n              className={cn(\n                \"ml-0.5 flex size-4 shrink-0 items-center justify-center rounded-sm text-muted-foreground/60\",\n                \"opacity-0 transition-opacity group-hover:opacity-100 focus-visible:opacity-100\",\n                \"hover:bg-accent hover:text-foreground\",\n                active && \"opacity-70\",\n              )}\n            >\n              ×\n            </button>\n          </div>\n        );\n      })}\n    </div>\n  );\n}\n"},{"path":"registry/ui/workspace-stack.tsx","source":"/**\n * Renders one stack: its tab strip plus the active view. Placement-agnostic —\n * the same component renders a stack in the tiled tree, a floating window, or\n * a popout surface.\n */\n\nimport * as React from \"react\";\n\nimport { cn } from \"@/lib/utils\";\nimport {\n  useViewComponent,\n  useWorkspaceDispatch,\n  useWorkspaceState,\n} from \"@/registry/hooks/use-workspace\";\nimport { useWorkspaceDrag } from \"@/registry/hooks/use-workspace-drag\";\nimport { WorkspaceTabList } from \"@/registry/ui/workspace-tab-list\";\nimport { WorkspaceDropOverlay } from \"@/registry/ui/workspace-drop-overlay\";\nimport type { WorkspaceState } from \"@/registry/lib/workspace/types\";\n\nexport interface WorkspaceStackProps\n  extends React.HTMLAttributes<HTMLDivElement> {\n  stackId: string;\n}\n\nexport function WorkspaceStack({\n  stackId,\n  className,\n  ...props\n}: WorkspaceStackProps) {\n  const dispatch = useWorkspaceDispatch();\n  const dragContext = useWorkspaceDrag();\n\n  // Drop-target handlers (early-return without an active drag). Mirrors\n  // tiling-tabs: dragover computes the edge live, drop dispatches view/move.\n  function onDragOver(event: React.DragEvent<HTMLDivElement>) {\n    if (!dragContext?.drag) return;\n    event.preventDefault();\n    event.dataTransfer.dropEffect = \"move\";\n    dragContext.updateDropTarget(\n      stackId,\n      { x: event.clientX, y: event.clientY },\n      event.currentTarget,\n    );\n  }\n\n  function onDragLeave(event: React.DragEvent<HTMLDivElement>) {\n    if (!dragContext?.drag) return;\n    const next = event.relatedTarget;\n    if (!(next instanceof Node) || !event.currentTarget.contains(next)) {\n      dragContext.clearDropTarget();\n    }\n  }\n\n  function onDrop(event: React.DragEvent<HTMLDivElement>) {\n    const drag = dragContext?.drag;\n    if (!dragContext || !drag) return;\n    event.preventDefault();\n    const edge =\n      dragContext.dropTarget?.stackId === stackId\n        ? dragContext.dropTarget.edge\n        : \"center\";\n    dispatch({\n      type: \"view/move\",\n      viewId: drag.viewId,\n      targetStackId: stackId,\n      edge,\n    });\n    dragContext.endDrag();\n  }\n\n  return (\n    <div\n      className={cn(\n        \"relative flex min-h-0 min-w-0 flex-col overflow-hidden rounded-md border bg-background\",\n        className,\n      )}\n      data-stack-id={stackId}\n      onDragOver={onDragOver}\n      onDragLeave={onDragLeave}\n      onDrop={onDrop}\n      {...props}\n    >\n      <StackBody stackId={stackId} />\n      {dragContext?.drag && dragContext.dropTarget?.stackId === stackId ? (\n        <WorkspaceDropOverlay edge={dragContext.dropTarget.edge} />\n      ) : null}\n    </div>\n  );\n}\n\n/** Subscribes to the store so the shell above doesn't re-render on changes. */\nfunction StackBody({ stackId }: { stackId: string }) {\n  const state = useWorkspaceState();\n  const stack = state.stacks[stackId];\n  if (!stack) return null;\n\n  const activeView = stack.activeViewId\n    ? state.views[stack.activeViewId]\n    : undefined;\n\n  return (\n    <>\n      <WorkspaceTabList stackId={stackId} />\n      <StackContent view={activeView ?? null} />\n    </>\n  );\n}\n\nfunction StackContent({\n  view,\n}: {\n  view: WorkspaceState[\"views\"][string] | null;\n}) {\n  const View = useViewComponent(view?.type ?? \"\");\n\n  return (\n    <div className=\"min-h-0 flex-1 overflow-auto\">\n      {view && View ? (\n        <View view={view} />\n      ) : (\n        <div className=\"flex h-full items-center justify-center p-4 text-center text-sm text-muted-foreground\">\n          {view\n            ? `No component registered for view type \"${view.type}\".`\n            : \"No open views.\"}\n        </div>\n      )}\n    </div>\n  );\n}\n"},{"path":"registry/ui/workspace-resize-handle.tsx","source":"/**\n * Draggable divider between the two children of a split. Dispatches\n * layout/resize on every move so state (and the state inspector) stays live.\n *\n * Also supports keyboard resize (arrow keys) and double-click to reset to\n * an even split.\n */\n\nimport * as React from \"react\";\n\nimport { cn } from \"@/lib/utils\";\nimport { useWorkspaceDispatch } from \"@/registry/hooks/use-workspace\";\n\nexport interface WorkspaceResizeHandleProps {\n  splitId: string;\n  direction: \"horizontal\" | \"vertical\";\n  ratio: number;\n  /** Step per arrow-key press, as a fraction of the container. */\n  keyboardStep?: number;\n}\n\ninterface DragState {\n  startX: number;\n  startY: number;\n  startRatio: number;\n  /** Container extent (width for horizontal, height for vertical) in px. */\n  extent: number;\n}\n\nconst MIN_RATIO = 0.1;\nconst MAX_RATIO = 0.9;\n\nfunction clamp(ratio: number): number {\n  return Math.min(MAX_RATIO, Math.max(MIN_RATIO, ratio));\n}\n\nexport function WorkspaceResizeHandle({\n  splitId,\n  direction,\n  ratio,\n  keyboardStep = 0.05,\n}: WorkspaceResizeHandleProps) {\n  const dispatch = useWorkspaceDispatch();\n  const drag = React.useRef<DragState | null>(null);\n  const [dragging, setDragging] = React.useState(false);\n  const horizontal = direction === \"horizontal\";\n\n  function onPointerDown(event: React.PointerEvent<HTMLDivElement>) {\n    const parent = event.currentTarget.parentElement;\n    if (!parent) return;\n    const rect = parent.getBoundingClientRect();\n    const extent = horizontal ? rect.width : rect.height;\n    if (extent <= 0) return;\n\n    drag.current = {\n      startX: event.clientX,\n      startY: event.clientY,\n      startRatio: ratio,\n      extent,\n    };\n    setDragging(true);\n    // Keep receiving move events when the cursor leaves the handle.\n    event.currentTarget.setPointerCapture(event.pointerId);\n    event.preventDefault();\n  }\n\n  function onPointerMove(event: React.PointerEvent<HTMLDivElement>) {\n    const state = drag.current;\n    if (!state) return;\n    const delta = horizontal\n      ? event.clientX - state.startX\n      : event.clientY - state.startY;\n    dispatch({\n      type: \"layout/resize\",\n      splitId,\n      ratio: clamp(state.startRatio + delta / state.extent),\n    });\n  }\n\n  function endDrag() {\n    drag.current = null;\n    setDragging(false);\n  }\n\n  function onKeyDown(event: React.KeyboardEvent<HTMLDivElement>) {\n    const forward = horizontal ? \"ArrowRight\" : \"ArrowDown\";\n    const back = horizontal ? \"ArrowLeft\" : \"ArrowUp\";\n    if (event.key !== forward && event.key !== back) return;\n    event.preventDefault();\n    const step = event.key === forward ? keyboardStep : -keyboardStep;\n    dispatch({ type: \"layout/resize\", splitId, ratio: clamp(ratio + step) });\n  }\n\n  return (\n    <div\n      role=\"separator\"\n      aria-orientation={horizontal ? \"vertical\" : \"horizontal\"}\n      aria-valuenow={Math.round(ratio * 100)}\n      aria-valuemin={MIN_RATIO * 100}\n      aria-valuemax={MAX_RATIO * 100}\n      aria-label=\"Resize panes\"\n      tabIndex={0}\n      onPointerDown={onPointerDown}\n      onPointerMove={onPointerMove}\n      onPointerUp={endDrag}\n      onPointerCancel={endDrag}\n      onKeyDown={onKeyDown}\n      onDoubleClick={() =>\n        dispatch({ type: \"layout/resize\", splitId, ratio: 0.5 })\n      }\n      className={cn(\n        \"group flex shrink-0 touch-none items-center justify-center rounded outline-none select-none\",\n        \"focus-visible:ring-ring/50 focus-visible:ring-2\",\n        horizontal ? \"w-1.5 cursor-ew-resize\" : \"h-1.5 cursor-ns-resize\",\n        dragging && \"bg-ring/20\",\n      )}\n    >\n      {/* Visible line; expands to a wider/narrower hit area. */}\n      <span\n        className={cn(\n          \"rounded-full bg-border transition-colors\",\n          \"group-hover:bg-ring/60 group-focus-visible:bg-ring\",\n          horizontal ? \"h-full w-px\" : \"h-px w-full\",\n        )}\n      />\n    </div>\n  );\n}\n"},{"path":"registry/ui/floating-window.tsx","source":"/**\n * Floating window shell. Works controlled (position/size props + change\n * callbacks — geometry owned by workspace state) or uncontrolled (default*\n * props + internal state — standalone usage like the floating-window demo).\n *\n * Chrome (title bar) is optional: frameless windows are moved by holding\n * the configured modifier key (e.g. ctrl) and dragging anywhere.\n */\n\nimport * as React from \"react\";\n\nimport { cn } from \"@/lib/utils\";\nimport type { ModifierKey } from \"@/registry/lib/workspace/keymap\";\nimport { modifierToEventKey } from \"@/registry/lib/workspace/keymap\";\n\ntype ResizeEdge = \"n\" | \"s\" | \"e\" | \"w\" | \"ne\" | \"nw\" | \"se\" | \"sw\";\n\ninterface Position {\n  x: number;\n  y: number;\n}\n\ninterface Size {\n  width: number;\n  height: number;\n}\n\ninterface DragState {\n  edge: \"move\" | ResizeEdge;\n  startX: number;\n  startY: number;\n  originPosition: Position;\n  originSize: Size;\n}\n\nexport interface FloatingWindowProps\n  extends Omit<React.HTMLAttributes<HTMLDivElement>, \"onDragStart\"> {\n  title?: string;\n  /** Controlled position/size — geometry lives outside the component. */\n  position?: Position;\n  size?: Size;\n  /** Uncontrolled fallbacks used when position/size are omitted. */\n  defaultPosition?: Position;\n  defaultSize?: Size;\n  onPositionChange?: (position: Position) => void;\n  onSizeChange?: (size: Size) => void;\n  /** Show the title bar? Default true. */\n  chrome?: boolean;\n  /** Modifier held on pointer-down anywhere to move the window. */\n  dragModifier?: ModifierKey | null;\n  /** Stacking order applied as z-index. */\n  zIndex?: number;\n  onClose?: () => void;\n  /** Override the content area padding (e.g. \"p-0\" to fill edge to edge). */\n  contentClassName?: string;\n  minWidth?: number;\n  minHeight?: number;\n}\n\nconst resizeHandles: { edge: ResizeEdge; className: string }[] = [\n  { edge: \"n\", className: \"inset-x-3 top-0 h-1.5 cursor-ns-resize\" },\n  { edge: \"s\", className: \"inset-x-3 bottom-0 h-1.5 cursor-ns-resize\" },\n  { edge: \"w\", className: \"inset-y-3 left-0 w-1.5 cursor-ew-resize\" },\n  { edge: \"e\", className: \"inset-y-3 right-0 w-1.5 cursor-ew-resize\" },\n  { edge: \"nw\", className: \"left-0 top-0 size-3.5 cursor-nwse-resize\" },\n  { edge: \"ne\", className: \"right-0 top-0 size-3.5 cursor-nesw-resize\" },\n  { edge: \"sw\", className: \"bottom-0 left-0 size-3.5 cursor-nesw-resize\" },\n  { edge: \"se\", className: \"bottom-0 right-0 size-3.5 cursor-nwse-resize\" },\n];\n\nfunction FloatingWindow({\n  title,\n  position: positionProp,\n  size: sizeProp,\n  defaultPosition = { x: 40, y: 40 },\n  defaultSize = { width: 320, height: 220 },\n  onPositionChange,\n  onSizeChange,\n  chrome = true,\n  dragModifier,\n  zIndex,\n  onClose,\n  contentClassName,\n  minWidth = 220,\n  minHeight = 140,\n  className,\n  children,\n  onPointerDown,\n  ...props\n}: FloatingWindowProps) {\n  // Controlled when position/size are given; internal state otherwise.\n  const [internalPosition, setInternalPosition] =\n    React.useState<Position>(defaultPosition);\n  const [internalSize, setInternalSize] = React.useState<Size>(defaultSize);\n  const position = positionProp ?? internalPosition;\n  const size = sizeProp ?? internalSize;\n  const [dragging, setDragging] = React.useState(false);\n  const drag = React.useRef<DragState | null>(null);\n\n  function updatePosition(next: Position) {\n    if (onPositionChange) onPositionChange(next);\n    else setInternalPosition(next);\n  }\n\n  function updateSize(next: Size) {\n    if (onSizeChange) onSizeChange(next);\n    else setInternalSize(next);\n  }\n\n  function beginDrag(edge: DragState[\"edge\"], event: React.PointerEvent) {\n    event.preventDefault();\n    drag.current = {\n      edge,\n      startX: event.clientX,\n      startY: event.clientY,\n      originPosition: position,\n      originSize: size,\n    };\n    setDragging(true);\n    // Capture the pointer so move/up events keep firing over this window\n    // even when the cursor leaves it.\n    event.currentTarget.setPointerCapture(event.pointerId);\n  }\n\n  function onRootPointerDown(event: React.PointerEvent<HTMLDivElement>) {\n    if (dragModifier && event[modifierToEventKey(dragModifier)]) {\n      beginDrag(\"move\", event);\n    }\n    onPointerDown?.(event);\n  }\n\n  function onPointerMove(event: React.PointerEvent) {\n    const state = drag.current;\n    if (!state) return;\n    const dx = event.clientX - state.startX;\n    const dy = event.clientY - state.startY;\n\n    if (state.edge === \"move\") {\n      updatePosition({\n        x: state.originPosition.x + dx,\n        y: state.originPosition.y + dy,\n      });\n      return;\n    }\n\n    let { x, y } = state.originPosition;\n    let { width, height } = state.originSize;\n\n    // Grow/shrink from the dragged edge, clamping to the minimum size.\n    // Edges that pull the opposite side also translate the window so the\n    // anchored edge stays put.\n    if (state.edge.includes(\"e\")) {\n      width = Math.max(minWidth, state.originSize.width + dx);\n    }\n    if (state.edge.includes(\"s\")) {\n      height = Math.max(minHeight, state.originSize.height + dy);\n    }\n    if (state.edge.includes(\"w\")) {\n      width = Math.max(minWidth, state.originSize.width - dx);\n      x = state.originPosition.x + (state.originSize.width - width);\n    }\n    if (state.edge.includes(\"n\")) {\n      height = Math.max(minHeight, state.originSize.height - dy);\n      y = state.originPosition.y + (state.originSize.height - height);\n    }\n\n    updatePosition({ x, y });\n    updateSize({ width, height });\n  }\n\n  function endDrag() {\n    drag.current = null;\n    setDragging(false);\n  }\n\n  return (\n    <div\n      style={{\n        left: position.x,\n        top: position.y,\n        width: size.width,\n        height: size.height,\n        zIndex,\n      }}\n      className={cn(\n        \"absolute flex flex-col overflow-hidden rounded-lg border bg-background shadow-lg\",\n        dragging && \"select-none\",\n        className,\n      )}\n      onPointerDown={onRootPointerDown}\n      onPointerMove={onPointerMove}\n      onPointerUp={endDrag}\n      onPointerCancel={endDrag}\n      {...props}\n    >\n      {chrome ? (\n        <div\n          onPointerDown={(event) => beginDrag(\"move\", event)}\n          className=\"flex h-9 shrink-0 touch-none items-center justify-between border-b bg-muted/50 px-3 active:cursor-grabbing\"\n        >\n          <span className=\"cursor-grab text-sm font-medium\">{title}</span>\n          {onClose ? (\n            <button\n              type=\"button\"\n              aria-label={`Close ${title ?? \"window\"}`}\n              onClick={onClose}\n              onPointerDown={(event) => event.stopPropagation()}\n              className=\"flex size-4 items-center justify-center rounded-sm text-muted-foreground/70 hover:bg-accent hover:text-foreground\"\n            >\n              ×\n            </button>\n          ) : (\n            <span aria-hidden className=\"flex gap-1\">\n              <i className=\"size-1.5 rounded-full bg-muted-foreground/40\" />\n              <i className=\"size-1.5 rounded-full bg-muted-foreground/40\" />\n              <i className=\"size-1.5 rounded-full bg-muted-foreground/40\" />\n            </span>\n          )}\n        </div>\n      ) : null}\n      <div className={cn(\"flex-1 overflow-auto p-4\", contentClassName)}>\n        {children}\n      </div>\n      {resizeHandles.map((handle) => (\n        <div\n          key={handle.edge}\n          onPointerDown={(event) => beginDrag(handle.edge, event)}\n          className={cn(\"absolute touch-none\", handle.className)}\n        />\n      ))}\n    </div>\n  );\n}\n\nexport { FloatingWindow };\n"},{"path":"registry/ui/workspace-drop-overlay.tsx","source":"/**\n * Drop preview overlay for a stack, rendered while a view is dragged over\n * it — shows where the view will land BEFORE it drops (ported from\n * tiling-tabs): center = tab into the stack, edges = split at that edge.\n */\n\nimport { cn } from \"@/lib/utils\";\nimport type { DropEdge } from \"@/registry/lib/workspace/drop-edge\";\n\nexport interface WorkspaceDropOverlayProps {\n  edge: DropEdge;\n}\n\nexport function WorkspaceDropOverlay({ edge }: WorkspaceDropOverlayProps) {\n  return (\n    <div\n      aria-hidden=\"true\"\n      data-drop-edge={edge}\n      className={cn(\n        \"pointer-events-none absolute z-20 border-2 border-primary/50 bg-primary/10\",\n        edge === \"center\" && \"inset-2\",\n        edge === \"left\" && \"inset-y-2 left-2 w-[calc(50%-0.5rem)]\",\n        edge === \"right\" && \"inset-y-2 right-2 w-[calc(50%-0.5rem)]\",\n        edge === \"top\" && \"inset-x-2 top-2 h-[calc(50%-0.5rem)]\",\n        edge === \"bottom\" && \"inset-x-2 bottom-2 h-[calc(50%-0.5rem)]\",\n      )}\n    />\n  );\n}\n"}], Component: resolveExport(WorkspaceDemoModule, "WorkspaceDemo") },
  { name: "example-with-css", title: "Example with CSS", description: "A login form with a CSS file.", files: [{"path":"registry/blocks/example-with-css/example-card.tsx","source":"import \"./example-card.css\"\n\nexport function ExampleCard() {\n  return (\n    <div className=\"login-container\">\n      <div className=\"login-card\">\n        <h1 className=\"login-title\">Login</h1>\n        <p className=\"login-subtitle\">\n          Please enter your credentials to continue\n        </p>\n        <form className=\"login-form\">\n          <div className=\"form-group\">\n            <label htmlFor=\"field-email\">Email</label>\n            <input\n              id=\"field-email\"\n              type=\"email\"\n              placeholder=\"Enter your email\"\n              required\n            />\n          </div>\n          <div className=\"form-group\">\n            <label htmlFor=\"field-password\">Password</label>\n            <input\n              id=\"field-password\"\n              type=\"password\"\n              placeholder=\"Enter your password\"\n              required\n            />\n          </div>\n          <div className=\"form-actions\">\n            <button type=\"submit\" className=\"login-button\">\n              Sign In\n            </button>\n          </div>\n          <div className=\"form-footer\">\n            <a href=\"#\" className=\"forgot-password\">\n              Forgot password?\n            </a>\n          </div>\n        </form>\n      </div>\n    </div>\n  )\n}\n"},{"path":"registry/blocks/example-with-css/example-card.css","source":".login-container {\n  --primary-color: #111111;\n  --primary-hover: #484747;\n  --error-color: #ef4444;\n  --text-color: #1f2937;\n  --text-light: #6b7280;\n  --border-color: #e5e7eb;\n  --background-light: #f9fafb;\n  --card-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1),\n    0 2px 4px -1px rgba(0, 0, 0, 0.06);\n\n  display: flex;\n  justify-content: center;\n  align-items: center;\n  padding: 1rem;\n  background-color: var(--background-light);\n  width: 100%;\n}\n\n.login-card {\n  width: 100%;\n  max-width: 400px;\n  padding: 2rem;\n  background-color: white;\n  border-radius: 8px;\n  box-shadow: var(--card-shadow);\n}\n\n.login-title {\n  margin: 0 0 0.5rem 0;\n  font-size: 1.5rem;\n  font-weight: 600;\n  color: var(--text-color);\n}\n\n.login-subtitle {\n  margin: 0 0 1.5rem 0;\n  font-size: 0.875rem;\n  color: var(--text-light);\n}\n\n.login-form {\n  display: flex;\n  flex-direction: column;\n  gap: 1.25rem;\n}\n\n.login-error {\n  padding: 0.75rem;\n  margin-bottom: 0.5rem;\n  background-color: rgba(239, 68, 68, 0.1);\n  border-left: 3px solid var(--error-color);\n  color: var(--error-color);\n  font-size: 0.875rem;\n  border-radius: 4px;\n}\n\n.form-group {\n  display: flex;\n  flex-direction: column;\n  gap: 0.5rem;\n}\n\n.form-group label {\n  font-size: 0.875rem;\n  font-weight: 500;\n  color: var(--text-color);\n}\n\n.form-group input {\n  padding: 0.75rem;\n  border: 1px solid var(--border-color);\n  border-radius: 4px;\n  font-size: 1rem;\n  transition: border-color 0.15s ease;\n}\n\n.form-group input:focus {\n  outline: none;\n  border-color: var(--primary-color);\n  box-shadow: 0 0 0 3px rgba(79, 70, 229, 0.1);\n}\n\n.form-group input::placeholder {\n  color: var(--text-light);\n}\n\n.form-actions {\n  margin-top: 0.5rem;\n}\n\n.login-button {\n  width: 100%;\n  padding: 0.75rem;\n  background-color: var(--primary-color);\n  color: white;\n  border: none;\n  border-radius: 4px;\n  font-size: 1rem;\n  font-weight: 500;\n  cursor: pointer;\n  transition: background-color 0.15s ease;\n}\n\n.login-button:hover {\n  background-color: var(--primary-hover);\n}\n\n.login-button:focus {\n  outline: none;\n  box-shadow: 0 0 0 3px rgba(79, 70, 229, 0.3);\n}\n\n.form-footer {\n  display: flex;\n  justify-content: center;\n  margin-top: 1rem;\n}\n\n.forgot-password {\n  font-size: 0.875rem;\n  color: var(--primary-color);\n  text-decoration: none;\n}\n\n.forgot-password:hover {\n  text-decoration: underline;\n}\n\n@media (max-width: 480px) {\n  .login-card {\n    padding: 1.5rem;\n  }\n}\n"}], Component: resolveExport(ExampleWithCssModule, "ExampleCard") },
];
